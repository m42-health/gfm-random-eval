program: main_biotype.py
method: grid
parameters:
  pool:
    values: ["max"]
  tokenizer:
    values: ["default"]
  pretrained:
    values: [false, true]
  use_local_data:
    values: [false, true]
  model_name:
    values: ["hyenadna"]
  embedding_dim:
    values: [-1]
